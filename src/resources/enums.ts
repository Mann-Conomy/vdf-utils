/**
 * Enum for JSON object notation used in parsing strings into objects.
 */
export enum ObjectNotation {
    EmptyObject = "{}",
    OpeningBrace = "{",
    ClosingBrace = "}",
}

/**
 * Enum for line separators used in converting objects to VDF string.
 */
export enum StringSeparator {
    Empty = "",
}
